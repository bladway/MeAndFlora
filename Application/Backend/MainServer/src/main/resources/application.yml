spring:
  application:
    name: MainServer
  profiles:
    include: hidden
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/MeAndFlora
    username: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql

docker:
  login: bladway
