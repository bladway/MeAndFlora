spring:
  application:
    name: MainServer
  profiles:
    include: hidden
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://25.41.235.94:5432/MeAndFlora
    username: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

docker:
  repository: bladway/maf-mainserver
  tag: 1.0
  login: bladway
